line-length = 110

[lint]
# Keep this a basic correctness/style gate (pyflakes + pycodestyle errors).
# Blind `except Exception` is a deliberate choice in main.py's main loop -
# a long-running sensor daemon must never crash on an unexpected error.
select = ["E4", "E7", "E9", "F"]
